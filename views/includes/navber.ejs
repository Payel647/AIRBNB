<style>
  .btn-search{
    background-color: #fe424d;
    color:#fff;
    border-radius: 25px;
  }
  .btn-search:hover{
    background-color: #fe424d;
    color:#fff;
  }

  .search-btn{
    border-radius: 25px;
    padding: 0.5rem 3rem 0.5rem 3rem;
    font-size: 0.8rem;
  }
  @media (max-width: 768px) {
      .search-input {
          width: 100%;
      }
  
      .search-btn {
          width: 100%;
          margin-top: 0.5rem;
      }
  
      .navbar-nav {
          width: 100%;
      }
  
      .navbar-nav .nav-link {
          padding: 0.7rem;
          width: 100%;
      }
  }
</style>
<nav class="navbar navbar-expand-md sticky-top">
    <div class="container-fluid">
      <a class="navbar-brand" href="/listings">
        <i class="fa-solid fa-house-circle-check"></i>
        <span>BookMyStay</span>
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav">
          <a class="nav-link" href="/listings">Explore</a>
        </div>
        <nav class="navbar ms-auto">
          <div class="container-fluid">
            <form class="d-flex" role="search" action="/listings/search" method="GET">
              <input class="form-control me-2 search-btn" type="search" placeholder="Search destinations" aria-label="Search" name="query">
              <button class="btn btn-search" type="submit">Search</button>
            </form>
          </div>
        </nav>
        <div class="navbar-nav ms-auto">
          <a class="nav-link" href="/listings/new">List your stay</a>
          <% if(currUser){ %>
            <a class="nav-link" href="/bookings"><b>My bookings</b></a>
          <% } %>
          <% if(!currUser){ %>
            <a class="nav-link" href="/signup"><b>Sign up</b></a>
            <a class="nav-link" href="/login"><b>Log in</b></a>
          <% } %> <% if(currUser){ %>
          <a class="nav-link" href="/logout"><b>Log out</b></a>
          <% } %>
        </div>
      </div>
    </div>
  </nav>